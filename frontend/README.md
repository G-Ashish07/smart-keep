# SmartKeep Frontend

React frontend for the SmartKeep FastAPI backend.

## Run locally

```bash
cd frontend
npm install
npm run dev
```

The app expects the backend at `http://localhost:8000`. Override with:

```bash
VITE_API_BASE_URL=http://localhost:8000 npm run dev
```
